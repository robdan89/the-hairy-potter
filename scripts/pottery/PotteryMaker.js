const id = 0;

const PotteryMaker = (shape, height, weight) => {
  id + 1;
  const newPotteryObj = {
    id: id,
    shape: shape,
    height: height,
    weight: weight,
  };
  return newPotteryObj;
};
